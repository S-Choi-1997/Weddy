export interface VendorInfo {
  name: string,
  businessNumber: string,
  phone: string
};

export interface VendorProductInfo {
  name: string,
  type: string,
  price: number,
  address: string,
  images: string[]
};