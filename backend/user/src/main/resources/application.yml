spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://34.64.98.78:3306/weddy?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: user1
    password: 123123123
  application:
    name: weddy
  profiles:
    active: common, user
  config:
    import: optional:configserver:http://15.164.159.172:8888
  cloud:
    gcp:
      storage:
        bucket: weddy-bucket-01
        project-id: rational-genius-434702-v8
        credentials:
          location: classpath:rational-genius-434702-v8-60dfa76b3911.json


  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include: refresh
# 이거 나중에 바꿔야함
server:
  port: 8080
