spring:
  jpa:
    show-sql: true
  application:
    name: weddy
  profiles:
    active: common , schedule
  config:
    import: optional:configserver:http://15.164.159.172:8888


management:
endpoints:
    web:
      exposure:
        include: refresh



consumer:
  topic1:
    name: contract_payment
  groupid:
    name: book
server:
  port: 8082
